model:
  name: meta-llama/Llama-3.2-3B
  torch_dtype: float16
  device: cuda
lora:
  r: 16
  lora_alpha: 32
  lora_dropout: 0.1
  target_modules:
  - q_proj
  - v_proj
  bias: none
training:
  local_epochs: 1
  batch_size: 2
  gradient_accumulation_steps: 8
  learning_rate: 0.0001
  weight_decay: 0.01
  max_seq_length: 256
  lora_param_dtype: float32
federated:
  num_rounds: 15
  num_clients: 10
  clients_per_round: 10
  aggregation_method: fedit
evaluation:
  eval_every: 5
  eval_batch_size: 4
  metrics:
  - loss
  - perplexity
checkpointing:
  save_every: 5
  keep_last_n: 1
seed: 42
data:
  dataset_name: databricks/databricks-dolly-15k
  dataset_split: train
  max_samples: 3000
  partition_method: iid
  eval_split: null
  eval_samples: 300
  label_column: category
  require_label_column: true
experiment:
  name: vp_l3_fedit_iid
  description: Partition-variance study | fedit | LLaMA-3.2-3B | Dolly-3k | iid
